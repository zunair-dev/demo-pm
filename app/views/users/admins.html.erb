<div class='card-body'>
  <h4>All Admins</h4> <br>
  <% if @user.count == 0 %>
    <div class="card">
      <center><i>No employees available yet!</i></center>
    </div>
  <% else %>
    <table style="width:100%;">
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Designation</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <% @user.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= user.designation %></td>
          <td><%= link_to 'Show', user_path(user), class: 'btn btn-primary' %></td>
          <td><%= link_to 'Edit', edit_user_path(user), class: 'btn btn-secondary' %></td>
          <td><%= link_to 'Destroy', user, data: { confirm: "Are you sure?" }, method: :delete, class: 'btn btn-danger' %></td>
        </tr>
      <% end %>
    </table>
  <% end %>
</div>