<div class='card-body'>
  <div class="d-flex justify-content-between">
    <h3>All Tasks</h3>
    <%= link_to 'Download Status Report for All Tasks', tasks_employees_path(current_user, format: :pdf), class: 'btn btn-warning mb-2' %>
  </div>
  <%= render "employees/form" %>
</div>
