<div class='card-body'>
  <div class="d-flex justify-content-between">
    <h3>All Projects</h3>
    <%= link_to 'Download Status Report for All Projects', projects_employees_path(current_user, format: :pdf), class: 'btn btn-secondary mb-2' %>
  </div>
  <%= render "projects_form" %>
</div>
