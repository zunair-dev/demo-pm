<div class='card-body'>
  <div class="d-flex justify-content-between">
    <h3>Tasks Completed</h3>
    <%= link_to 'Download Status Report for Completed Tasks', complete_employees_path(current_user, format: :pdf), class: 'btn btn-secondary mb-2' %>
  </div>
  <%= render "employees/form" %>
</div>
