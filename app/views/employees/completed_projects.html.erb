<div class='card-body'>
  <div class="d-flex justify-content-between">
    <h3>Completed Projects</h3>
    <%= link_to 'Download Status Report for Completed Projects', employees_completed_projects_path(current_user, format: :pdf), class: 'btn btn-secondary mb-2' %>
  </div>
  <%= render "projects_form" %>
</div>
