<div class='card-body'>
  <div class="d-flex justify-content-between">
    <h3>Active Projects</h3>
    <%= link_to 'Download Status Report for Active Projects', employees_active_projects_path(current_user, format: :pdf), class: 'btn btn-secondary mb-2' %>
  </div>
  <%= render "projects_form" %>
</div>
